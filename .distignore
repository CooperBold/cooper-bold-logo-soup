# Git / GitHub (workflow stays in repo; not copied to SVN trunk)
/.git
/.github
.gitignore
.gitattributes
.distignore

# WordPress.org assets (handled separately by deploy action → SVN assets/)
/assets

# Plugin admin branding ships in the release ZIP (admin/images/cooper-bold-wordmark.png).
/.wordpress-org

# AI, wiki, and dev scaffolding
/wiki
/crewai
/docs
/.cursor
/README.md
/scripts
/dist
*.zip
AI_SESSION_MEMORY.md
AI_RUNBOOK.md
AGENTS.md

# SimpleMem
/simplemem_cli.py
/simplemem_client.py
/requirements.txt

# Node toolchain and source (ship compiled build/ only)
/node_modules
/package.json
/package-lock.json
/.wp-env.json
/src

# Environment and secrets
/.env
/.env.example
/.env.*

# Scratch and local-only paths
/uncommitted
.wp-seed-logos

# Python artifacts
__pycache__
*.py[cod]
.python-version
.venv
venv
crewai/.venv

# OS / editor
.DS_Store
.idea
.vscode

# PHP dev toolchain (not shipped to wp.org)
/composer.json
/composer.lock
/composer.phar
/composer-setup.php
/vendor
.phpunit.result.cache

# Tests
/tests
/phpunit.xml
/phpunit.xml.dist
